apiVersion: v1
kind: Service
metadata:
  name: {{ include "logmind.fullname" . }}-api
  labels:
    {{- include "logmind.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "logmind.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
